// script.js
function fadeIn() {
  var fadeText = document.getElementById('fade-text');
  fadeText.style.opacity = '1'; // 將透明度設置為 1，實現淡入效果
}

function fadeOut() {
  var fadeText = document.getElementById('fade-text');
  fadeText.style.opacity = '0'; // 將透明度設置為 0，實現淡出效果
}
